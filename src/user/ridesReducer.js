/*
const initialState = {
  rides: null
}

const ridesReducer = (state = initialState, action) => {
  if (action.type === 'RIDES_LOADED')
  {
    return Object.assign({}, state, {
      rides: action.payload
    })
  }

  if (action.type === 'USER_LOGGED_OUT')
  {
    return Object.assign({}, state, {
      rides: null
    })
  }

  return state
}

export default ridesReducer
*/